# paper2latex Web Interface

This directory contains the local web UI for the upstream PDF-to-LaTeX tool.

## Notes

- The web app is separate from the structured knowledge-base pipeline.
- It feeds into `tmp/paddle_output/`, which is then consumed by `knowledge_engineering/`.
- Keep temporary browser or build artifacts out of `data/`.
